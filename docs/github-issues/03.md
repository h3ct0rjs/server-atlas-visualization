The production build reports a roughly 747 kB minified bundle. Geometry is shared in room servers, but repeated explorer changes and GPU resource lifetime remain unmeasured.

## Acceptance criteria

- [ ] Record baseline initial load, built chunk sizes, and rendering/resource measurements with browser/device context.
- [ ] Exercise repeated room/server/floor transitions and floor/cover/filter toggles; investigate sustained growth in geometry, materials, textures, listeners, or renderer resources.
- [ ] Split and lazy-load detail explorer code where it provides measurable benefit, accounting for geometry code also used by the room.
- [ ] Provide an accessible loading state and preserve direct hash routes and room state across transitions.
- [ ] Compare before/after results and document remaining limitations; run tests and production build.

## Relevant files

src/App.tsx, src/Scene.tsx, src/DetailExplorer.tsx, src/room-server.ts, vite.config.ts

Tracked from the unfinished Datacenter Atlas backlog in `todo.md`. Existing floor/rack integration is complete; this issue covers remaining work only.
